# outputs

実際の成果物のスライドを置く場所。

- 中身は **git 管理外**（この README だけを追跡している）。
- 1デッキ1フォルダ: `outputs/<deck-name>/sample.html`
- ルールと見本は `design-rule/`（`rules.html` が正本、`sample.html` が見本デッキ）。
- チェック: `python3 tools/check-rules.py outputs/<deck-name>/sample.html`
